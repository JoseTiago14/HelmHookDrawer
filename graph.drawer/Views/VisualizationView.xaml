<UserControl x:Class="graph.drawer.Views.VisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:graph.drawer.ViewModels"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:converters="clr-namespace:graph.drawer.Views.Render.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:VisualizationViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <converters:ChartNameDefaultBoxText x:Key="ChartNameDefaultBoxText" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Expander Grid.Row="0" Header="Settings" IsExpanded="True">
            <StackPanel Margin="0 10 10 10" Orientation="Horizontal" HorizontalAlignment="Left">

                <StackPanel>
                    <Label Content="Mode" Foreground="Gray" />
                    <ComboBox Height="20" Width="75" HorizontalAlignment="Left" Margin="10 3"
                              ItemsSource="{Binding Modes}"
                              SelectedItem="{Binding SelectedMode.Value}" />
                </StackPanel>
                <StackPanel Margin="15 0">
                    <Label Content="Charts" Foreground="Gray" />
                    <ItemsControl ItemsSource="{Binding Checkboxes.Value}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="viewModels:CheckBoxItem">
                                <CheckBox IsChecked="{Binding Checked.Value}"
                                          Content="{Binding ChartName,Converter={StaticResource ChartNameDefaultBoxText}}"
                                          Margin="10 3" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

            </StackPanel>
        </Expander>

        <ScrollViewer Grid.Row="1" HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Expander Header="Pre">
                    <ItemsControl ItemsSource="{Binding PreInstalls.Value}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl cal:View.Model="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>
                <Expander Header="Deploy">
                    <ItemsControl ItemsSource="{Binding Installs.Value}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl cal:View.Model="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>
                <Expander Header="Pos">
                    <ItemsControl ItemsSource="{Binding PostInstalls.Value}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl cal:View.Model="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="0" Grid.RowSpan="2"
              Visibility="{Binding ErrorMessageVisibility.Value}" Background="#F1F0F0">
            <TextBlock Text="{Binding ErrorMessage.Value}" Foreground="Gray"
                       VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Grid>
    </Grid>
</UserControl>