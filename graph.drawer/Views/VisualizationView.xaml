<UserControl x:Class="graph.drawer.Views.VisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:graph.drawer.Flow.Streams.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:VisualizationViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="50">

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0 10 10 10" Orientation="Horizontal"  HorizontalAlignment="Left">
            <Label Content="Mode" Foreground="Gray" />
            <ComboBox Height="20" Width="75">
                <ComboBox.Items>
                    <ComboBoxItem IsSelected="True">Install</ComboBoxItem>
                    <ComboBoxItem>Upgrade</ComboBoxItem>
                    <ComboBoxItem>Delete</ComboBoxItem>
                </ComboBox.Items>
            </ComboBox>
        </StackPanel>


        <ToggleButton Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Margin="10">
            <ToggleButton.Template>
                <ControlTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Button IsEnabled="{TemplateBinding ToggleButton.IsChecked}" Margin="5 0 0 0">List</Button>
                        <Button Margin="5 0 0 0"
                                IsEnabled="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType=ToggleButton},Path=IsFocused}"
                                IsHitTestVisible="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType=ToggleButton},Path=IsFocused}">
                            Tree
                        </Button>
                    </StackPanel>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
        <!--<StackPanel Grid.Row="0" Grid.Column="1" Margin="10" Background="OrangeRed" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <TextBlock Margin="5 0">List</TextBlock>
            <TextBlock Margin="5 0">Tree</TextBlock>
        </StackPanel>-->

        <!--<TextBlock  HorizontalAlignment="Center">Select a file to calculate its installation dependency</TextBlock>-->
        <TreeView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
            <TreeViewItem Header="pre installs">
                <TreeViewItem Header="gatekeeper release">
                    <ListBox>
                        <ListBoxItem>seeding</ListBoxItem>
                        <ListBoxItem>job</ListBoxItem>
                        <ListBoxItem>chron job</ListBoxItem>
                    </ListBox>
                </TreeViewItem>
                <TreeViewItem Header="server release">
                    <ListBox>
                        <ListBoxItem>migration</ListBoxItem>
                    </ListBox>
                </TreeViewItem>
            </TreeViewItem>
            <TreeViewItem Header="installs">
                <TreeViewItem Header="gatekeeper release">
                    <ListBox>
                        <ListBoxItem>pod</ListBoxItem>
                        <ListBoxItem>job</ListBoxItem>
                        <ListBoxItem>chron job</ListBoxItem>
                        <ListBoxItem>pod</ListBoxItem>
                    </ListBox>
                </TreeViewItem>
                <TreeViewItem Header="server release">
                    <ListBox>
                        <ListBoxItem>pod</ListBoxItem>
                    </ListBox>
                </TreeViewItem>
            </TreeViewItem>
            <TreeViewItem Header="post installs">
                <TreeViewItem Header="gatekeeper release">
                    <ListBox>
                        <ListBoxItem>job</ListBoxItem>
                        <ListBoxItem>job</ListBoxItem>
                        <ListBoxItem>pod</ListBoxItem>
                        <ListBoxItem>pod</ListBoxItem>
                    </ListBox>
                </TreeViewItem>
                <TreeViewItem Header="server release">
                    <ListBox>
                        <ListBoxItem>job</ListBoxItem>
                    </ListBox>
                </TreeViewItem>
            </TreeViewItem>
        </TreeView>
    </Grid>
</UserControl>